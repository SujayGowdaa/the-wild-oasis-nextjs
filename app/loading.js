// this component will be rendered for the parent and child components when the data is being fetched from the server
import Spinner from './_components/Spinner';

export default function loading() {
  return <Spinner />;
}
